CREATE TABLE AUTOR_PLANTA(
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    ID_PLANTA NUMBER NOT NULL,
    ID_AUTOR NUMBER NOT NULL,
    PRIMARY KEY(ID),
    FOREIGN KEY (ID_PLANTA) REFERENCES PLANTA(ID),
    FOREIGN KEY (ID_AUTOR) REFERENCES AUTOR(ID)
);
