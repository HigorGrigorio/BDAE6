CREATE TABLE BASONIMO_PLANTA(
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    ID_BASONIMO NUMBER NOT NULL,
    ID_PLANTA NUMBER NOT NULL,
    PRIMARY KEY(ID),
    FOREIGN KEY (ID_BASONIMO) REFERENCES BASONIMO(ID),
    FOREIGN KEY(ID_PLANTA) REFERENCES PLANTA(ID)
);